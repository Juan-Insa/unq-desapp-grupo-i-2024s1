
name: Test & Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
            java-version: '17'

      - name: Build with Gradle
        run: ./unq-desapp-grupo-i-2024s1/gradlew build --no-daemon

      - name: Run tests
        run: ./unq-desapp-grupo-i-2024s1/gradlew test --no-daemon
        
      - name: Run a one-line script
        run: echo Hello, world!
